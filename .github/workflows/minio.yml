name: minio
on:
  workflow_dispatch:
jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - name: Set bash working dir
      run: |
       echo "jamil"


    - uses: actions/checkout@v3    
 
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.20'

    - name: Build
      run: go build -v 

    - name: Test
      run: go test -v ./math/

      shell: bash 

 